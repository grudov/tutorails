<h1>Inscription</h1>

<%= form_for(@user) do |f| %>

  <%= render 'shared/error_messages', object: f.object %>

  <%= render 'form', f: f %>
  
  <div class="actions">
    <%= f.submit "Inscription" %>
  </div>
<% end %>